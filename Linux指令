kill 命令，结束进程；
sleep，使某一进程睡眠，无法工作；
gzip 命令，压缩文件，压缩后源文件会被删除；且后缀为.gz
解压时，命令为 gzip -d +文件；


打包且压缩目录用 tar
tar -c 压缩，-z解压缩，-x解包，先解压缩再解包



zip 命令，







vi/vim(升级版） 编译器；
vi 适用于文本编辑，vim 适用于写代码；

vi 后接文件名、可以vi的形式直接打开该文件；
进入后有不同的模式，插入模式（可以插入信息），命令模式，转义模式后接 ：（存储且退出wq） （q！不保存，直接退出）


命令模式，数字加G跳转到 该数字的行数；

移动到下一个单词的词头，w W;
移动到下一个单词的结尾，e E;

转义模式下的命令：
设置行号; set nu; 取消时 ：set unnu;
定位到某一行： 数字+G；
或者直接写数字；、
 


修改文本；

删除字符；x 光标所在处的字符；
	   X 光标前所在的字符；
 删除整行： dd;
		删除该行光标后的所有字符 D；

复原命令： u 恢复到上一步；
	    U 恢复所有修改；

> 将文本向右移一段距离，相当于word中的tab健；
<将文本向右移一段距离，相当于word中的tab健；


