文件类型：
d 目录
l 符号链接
s 套接字文件；
p 管道文件；

 ln 命令
创建硬链接，

s 命令创建软链接；


硬链接特征；
I节点相同


修改文件权限： chmod    chmond key 文件名
 操作符：  +  -  =；
key 由 who +操作符 组成


chmod +三位数字+ 文件名 
 chmod -R +三位数字+文件名 ；将该文件夹下所有的文件权限修改到相同权限；

grep 作用： 查询关于包含某字符串的文件；
语法： grep 查询方式  +关键字+文件名；


sort 排列后并未修改原文件信息；


sort 后可接多个文件 进行排序‘
结果还是数字在前，字母在后；




uniq 命令 ，删除重复行，当重复行不相邻时，uniq命令不起作用；
  -c 表示 相邻重复行的个数；
 -d 只显示重复行；

comm 命令；对已经排好序的文件进行比较；
语法：comm +文件+文件 
结果 第一列为 文件1 独有的；
第二列为文件2 独有的；
第三列为两文件共有的； 

 -（1或2或3） 表示不显示第一列，显示其他两列；

diff 命令：比较两个文本文件并找出他们的不同；
diff + 文件 +文件；

-b 忽略空格的差别；
-i 不区别大小写区别；

find 命令：在指定范围下查找相应的文件；
格式： find+/搜索范围+搜索文件；
-name 按照文件名查找；
-iname 按照文件名不区分大小写来查找；
-size 按照文件大小查找；

-a- 表示逻辑与；
-o- 表示逻辑或；

外置命令用 man 查看；
内置命令用help查看；
 

  -ps  有关服务器进程的命令

UID:该进程执行的用户id；
PID:进程id;
PPID该进程的父进程；
c cpu占用率；


kill 格式：

先关父进程再关子进程；
